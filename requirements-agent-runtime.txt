# Dependencias del codigo del agente Windows (solo lo que empaqueta PyInstaller).
# Instalar en el MISMO intérprete que ejecuta pyinstaller, antes de compilar:
#   python -m pip install -r requirements-agent-runtime.txt
httpx==0.27.0
dbfread==2.0.7
